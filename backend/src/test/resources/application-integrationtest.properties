# Usa o PostgreSQL do docker-compose já em execução (infra/docker-compose.yml)
# em vez de subir um container via Testcontainers.
spring.datasource.url=jdbc:postgresql://localhost:5432/halo
spring.datasource.username=halo
spring.datasource.password=halo
spring.flyway.enabled=true
